name: Build ThermoAI APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

  

      - name: Build APK
        run: ls -R

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ThermoAI-APK
          path: app/build/outputs/apk/debug/app-debug.apk
